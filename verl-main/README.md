# MulFeRL vendored training framework

This directory contains the `verl`-based implementation used by the anonymous
MulFeRL release. See the repository-level `README.md` for MulFeRL-specific
setup, data format, feedback-provider configuration, and training commands.

The upstream framework is retained only as the training substrate. Reward
functions, benchmark evaluators, and evaluation scripts are not part of the
anonymous release.
